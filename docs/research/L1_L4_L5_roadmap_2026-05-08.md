# UIDT Framework v3.9 — Master Research Roadmap (L1, L4, L5)

**Date:** 2026-05-08  
**Status:** Audit-Corrected Master Document / Active Research
**Evidence Category:** [A-]/[D]/[E-open] (research targets; no canonical evidence upgrade implied)

> [!WARNING]
> This document supersedes `L1_L4_L5_next_steps_2026-04-29.md`, which contained critical classification errors regarding L1 and L4.

This document serves as the master roadmap for resolving the core open limitations:
*   **L1:** 10¹⁰ geometric factor
*   **L4:** RG derivation of γ
*   **L5:** N=99 steps justification

## 1. Current Status (canonical ledger vs. research targets)

- γ (kinetic) = 16.339 remains **phenomenological [A-]** with **L4 open**, per
  `CANONICAL/CONSTANTS.md` and `CANONICAL/EVIDENCE_SYSTEM.md`. The special rule
  "γ MUST always be [A-], NEVER [A]" is in force until a scheme-independent
  first-principles derivation is registered in `LEDGER/CLAIMS.json`.

- **γ_bare = 49/3 (SU(3) color-algebra identity):** Treated as a
  **conjecture / research target**, currently **[E-open]**. It will be
  upgraded to [A] only after a complete LaTeX derivation with residual
  < 10⁻¹⁴ is documented and the corresponding claim is registered and
  approved in `LEDGER/CLAIMS.json` (dual-approval required per
  `CANONICAL/EVIDENCE_SYSTEM.md`).

- **Kill-switch Σ_T(E_T=0) = 0:** Remains a **consistency condition and
  research target**, currently **[A-]** (follows formally from the torsion
  kill-switch rule in UIDT-OS, but a standalone theorem file
  `docs/theory/Torsion_Self_Energy.md` with full proof and claim
  registration is required before [A] status is granted).

- **L1, L4, L5** remain open limitations. This roadmap document only tracks
  the research plan. It does **not** modify canonical evidence categories.
  No evidence upgrade is implied by its content alone.

## 2. Corrected Limitations Matrix

The previous roadmap erroneously conflated L1 (the geometric factor) with L4 (the $\gamma$ derivation). The definitive classification is:

| Limitation | Core Problem | Target Evidence | Current Status | Required Action / Next Step |
|---|---|---|---|---|
| **L1** | **$10^{10}$ Geometric Factor:** The gap between theoretical coupling and phenomenological lattice coupling. | [A] | [E-open] | Evaluate topological volume extensivity vs. bulk-boundary scaling (Path C: $N=99$ linkage). Independent derivation required. |
| **L4** | **Anomalous Dimension Derivation:** The $\gamma_{	ext{ledger}} = 16.339$ value lacks a full first-principles RG derivation. | [A] | [E-open] | BMW-Truncation FRG to prove 1-loop vacuum correction yields $\delta\gamma_{	ext{bare}} = 0.00567$. Full first-principles derivation, residual < 10⁻¹⁴, CLAIMS.json entry. |
| **L5** | **$N=99$ RG Steps:** The exact cutoff count is empirical. | [A-] | [E-open] | Explicit mathematical linkage of $\Sigma_T = f(E_T)$ to the cascade sequence. Physical justification or code alignment. |
| **γ_bare**| **49/3 identity:** treated as conjecture / research target. | [A] | [E-open] | LaTeX proof + CLAIMS.json dual-approval |
| **Σ_T=0**| **Kill-switch Σ_T = 0:** consistency condition and research target. | [A] | [A-] | Standalone theorem file + CLAIMS.json entry |

## 3. Disambiguation of $\delta\gamma$

Two distinct adjustments to $\gamma$ exist. They must never be conflated:

1.  **Finite-Size Scaling Dressing:** $\delta\gamma = 0.0047$ [B]
    *   Bridges $V 	o \infty$ continuum limits to actual phenomenological fits.
2.  **Color Algebra Gap:** $\delta\gamma_{	ext{bare}} = 0.00567$ [D]
    *   Bridges pure algebraic $SU(3)$ scaling ($\gamma_{	ext{bare}} = 49/3$) to the canonical value ($16.339$).

## 4. L4: FRG $\gamma$ Derivation Status (Updated)

The attempt to derive $\gamma$ from first-principles RG flow is the most complex active research vector.

| Path | Approach | Status |
|---|---|---|
| **A** | Direct numerical continuum limit | ❌ **Failed:** Diverges in IR |
| **B** | Pure scaling analysis (49/3 base) + 1-loop | 🔬 **Active:** Needs BMW Truncation |
| **C** | Perturbative loop expansion | ❌ **Failed:** Breaks down near $\Delta^*$ |
| **D1**| FRG with LPA' truncation (NLO) | ❌ **Failed:** $\lambda_3$ insufficient (Factor $2857	imes$ off) [NO-GO-STEP5] |
| **D2**| FRG with BMW momentum truncation | 🔬 **Active:** Requires massive computational resources |

## 5. Priority Matrix

1.  **Priority 1 (L4):** Implement BMW-Truncation for the FRG flow. The LPA' approach has definitively failed ([NO-GO-STEP5]). A momentum-dependent vertex is required to compute $\Delta\gamma_{	ext{1-loop}}$.
2.  **Priority 2 (L1):** Initiate formal dimensional analysis of the $10^{10}$ gap. Evaluate $e^{N/10}$ linkages.
3.  **Priority 3 (L5):** Formalize the $N=99$ sequence algebraically.

## 6. Recent Additions
*   **PR #427:** Documented planar degeneracy of the three-gluon vertex. This is strictly **FRG INPUT** (boundary conditions), not the FRG solution itself.
*   **Phase 1-3 Sprint:** Established rigorous `mpmath` boundaries for the $\delta\gamma_{	ext{bare}}$ calculation and surveyed lattice legacy data limits.
